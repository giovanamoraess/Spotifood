import * as React from "react";
import { Dictionary, AsElement, AsComponent } from "./_utils/types";
declare function as<T extends AsElement<any>>(asComponents: T | T[]): <P extends Dictionary<any>>(WrappedComponent: React.ComponentType<P>) => AsComponent<T, P>;
export default as;
